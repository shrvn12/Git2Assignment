function checkprime(num) {
    let factor = ;
    for(let x = 1; x <= num; x++) {
       if (num % x === ) {
          factor++;
       }

    }
      if (factor==1){
         console.log("Prime")
      }
      else{
      console.log("Not Prime") 
      }
}